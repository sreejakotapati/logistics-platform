# AI Services

Intelligence capabilities (ETA, route optimization, demand forecasting, anomaly detection) — Sprint 10, all behind feature flags with heuristic fallbacks.

- `pipelines/` — training data pipelines.
- `models/` — versioned models + model cards.
- `inference/` — inference APIs + monitoring.
