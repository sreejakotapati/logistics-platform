# Cross-cutting Tests

Platform-wide end-to-end journeys and the **cross-tenant isolation suite** (owned by the QA agent). Unit/integration tests live next to their code in `backend/tests` and `frontend`.
